export default function GoldHand3D({ className = "" }) {
  return (
    <div className={`gold-hand-3d-container ${className}`}>
      <span className="gold-hand-glow" />
      <span className="gold-hand-emoji">👆</span>
    </div>
  );
}
